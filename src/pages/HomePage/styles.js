import styled from 'styled-components'
import { Fab } from '../../components/Fab'

export const DetachedFab = styled(Fab)`
    && {
        margin: 24px;
    }
`

export const DetachedText = styled.span`
    margin: 0 4px;
`
